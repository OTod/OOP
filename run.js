const server = require("./server/server");
/// here is the starting script. Probably all the plugins can be written in here.
console.log("File server and REST server are starting.....")
// setTimeout(server, 1000);
server()